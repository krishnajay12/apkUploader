import 'package:flutter/material.dart';

class PrintPage80mm extends StatelessWidget {
  const PrintPage80mm({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}